// import type { HttpContextContract } from '@ioc:Adonis/Core/HttpContext'

export default class BoxesController {
  public async testbox(){

    const orange = [2,4,6]
    const apple = [1,3,7] 
    const both = [1,2,3,4,5,6,7]

    // without looking directly inside the box and taking out exactly one piece of fruit, how can you immediately label all of the boxes correctly?

    //if there is 2 consecutive  data from each one you can determine what container it belongs
    
  }
}
